# Stop Lombok looking further up the filesystem for configuration outside the repository.
config.stopBubbling = true

# Mark generated methods with @lombok.Generated so coverage tools do not report generated
# getters as untested code.
lombok.addLombokGeneratedAnnotation = true
