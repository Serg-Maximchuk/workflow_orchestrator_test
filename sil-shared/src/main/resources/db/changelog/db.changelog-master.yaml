databaseChangeLog:
  # Only the application's own tables live here. Flowable manages its ACT_* schema itself
  # (flowable.database-schema-update), so the two never fight over the same changelog.
  - include:
      file: db/changelog/001-idempotency-and-qualification.yaml
  - include:
      file: db/changelog/002-service-order.yaml
  - include:
      file: db/changelog/003-async-fulfilment.yaml
  - include:
      file: db/changelog/004-cancellation.yaml
